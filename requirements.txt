# Core dependencies
pyyaml>=6.0
python-dotenv>=1.0.0

# Optional AI service dependencies (install with: pip install batch-translator[all])
openai>=1.0.0
anthropic>=0.18.0
# Note: MiniMax is supported natively via REST API (no extra package needed)
